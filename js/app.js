const input=document.querySelector("#search");const form=document.querySelector(".search");const cards=document.querySelector("#cards");function render(list){if(!cards)return;cards.innerHTML=list.map(i=>`<a class="card" href="sites/${i.slug}.html"><span>${i.category}</span><h2>${i.name}</h2><p>${i.description}</p><small>${i.fit}</small></a>`).join("")}function search(){const q=(input?.value||"").toLowerCase().trim();render(q?SITE_ITEMS.filter(i=>(i.name+i.category+i.description+i.fit).toLowerCase().includes(q)):SITE_HOME)}input&&input.addEventListener("input",search);form&&form.addEventListener("submit",e=>{e.preventDefault();search();cards?.scrollIntoView({behavior:"smooth"})});