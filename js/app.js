const cards=document.querySelector("#cards");const catTarget=document.querySelector("#categoryGrid")||document.querySelector("#categories");const q=document.querySelector("#search");const cats=[...new Set(SITE_DATA.items.map(i=>i.category))];catTarget.innerHTML=cats.map(c=>`<a class="card" href="categories/${SITE_DATA.categorySlugs[c]}.html"><span class="tag">${c}</span><h2>${c}</h2><p>${SITE_DATA.items.filter(i=>i.category===c).length} UAE resources in this section.</p></a>`).join("");function render(list){cards.innerHTML=list.map(i=>`<a class="card" href="sites/${i.slug}.html"><span class="tag">${i.category}</span><h2>${i.name}</h2><p>${i.description}</p><p><strong>Best for:</strong> ${i.whoFor}</p></a>`).join("")}render(SITE_DATA.homeItems);q&&q.addEventListener("input",e=>{const s=e.target.value.toLowerCase();render(s?SITE_DATA.items.filter(i=>(i.name+i.category+i.description+i.whoFor).toLowerCase().includes(s)):SITE_DATA.homeItems)});